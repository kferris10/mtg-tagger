# Flask configuration
FLASK_SECRET_KEY=generate-a-secure-random-key-here-using-secrets-token-hex-32

# Anthropic API key (used server-side only, never exposed to users)
ANTHROPIC_API_KEY=sk-ant-...

# Shared access code for friends (optional — if not set, no code is required)
ACCESS_PASSWORD=your-chosen-access-code-here
