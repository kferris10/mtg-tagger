# Flask configuration
FLASK_SECRET_KEY=generate-a-secure-random-key-here-using-secrets-token-hex-32

# Anthropic OAuth configuration
ANTHROPIC_OAUTH_CLIENT_ID=your-client-id-from-anthropic-console

# OAuth redirect URI (update for production)
OAUTH_REDIRECT_URI=http://localhost:5000/oauth/callback

# Production settings (uncomment for production deployment):
# OAUTH_REDIRECT_URI=https://your-domain.com/oauth/callback
# FLASK_ENV=production
